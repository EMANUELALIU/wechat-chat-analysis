---
name: wechat-chat-analysis
description: "微信聊天记录导出与人物关系分析全流程。A. 技术导出：Mac 微信 4.x 数据库解密导出（lldb 抓密钥 + rmqg 解密导出，含微信 4.1.11 已验证的全部踩坑结论与分工方案）；B. 分析：单人物深度画像、多人横向对比、约会对象验真、婚恋决策建议；C. 输出：对比表、验真提问清单、试探话术、dating 日记改写。触发词：导出微信聊天记录、微信聊天导出、把聊天记录导出来、分析聊天记录、分析这个人/这个男生/这个女生/这个男人、对比这几个男生/女生/男人、dating 分析、约会对象分析、帮我看看和 XX 的聊天、人物关系分析、感情复盘"
---

# 微信聊天记录导出 + 人物关系分析

本 skill 覆盖一条完整链路：**从微信里把聊天记录导出 → 阅读分析 → 输出人物画像 / 横向对比 / 决策建议**。两部分可独立使用：用户直接给聊天文本时跳过导出；只想备份时跳过分析。

## 硬性原则（先读）

1. **只处理用户自己的账号、自己的数据、自己的聊天**。涉及解密的均为用户本人设备与账号，绝不用于他人数据。
2. 导出会解密本地数据库，全程不联网、不外传，密钥只存本机。
3. 分析输出必须**客观中立**：风险与加分项都列，不做恐吓式劝分，也不做恋爱脑式吹捧；最终结论给"验真动作"，不给"替用户做决定"。
4. 大原则（贯穿分析始终）：**头衔和收入可以包装，但住哪、开什么车、怎么花小钱不会说谎。**

## 触发判断

- 用户要"导出微信记录/聊天记录" → 走【阶段 A：导出】，见 `references/export-playbook.md`
- 用户要"分析某个人/对比几个人/复盘约会/看看这人靠不靠谱" → 走【阶段 B：分析】，见 `references/analysis-framework.md`
- 用户要"写成 dating 日记/小红书文案" → 见 `references/analysis-framework.md` 的输出章节
- 两者都要（如"导出并分析"）→ 先 A 后 B

## 阶段 A：导出（核心分工：sudo 步骤必须用户亲手跑）

流程总览（详细版见 `references/export-playbook.md`，首次执行请完整阅读）：

1. **环境侦查**：确认微信版本（4.x 还是 3.x）、数据目录、加密状态、是否 Apple Silicon。
2. **准备工具**：跑 `scripts/setup.sh`（建 venv、装依赖、克隆 rmqg/wechat-mac-export）。
3. **用户执行**：重签微信 + 抓密钥（`scripts/capture.sh`，需 sudo + 微信运行中滚动聊天 90 秒）。
4. **助手执行**（无需 sudo）：copy 数据 → match 密钥 → decrypt → export。
5. **交付**：按会话生成 txt + index.csv/index.html；按需为指定人单独生成 CSV + zip。

⚠️ 已踩过的坑（务必遵守，别再重走）：
- PyWxDump 2.x 依赖 pywin32，**Mac 装不了**；
- wcdb-key-tool 的 passphrase→PBKDF2(256000) 模型对 **4.1.11 已失效**；
- `key_info.db` 虽是明文，但其 `key_info_data` 被机器密钥加密，**4995 种派生组合全失败，别浪费时间**；
- 正确路线唯一：**lldb 断点 `CCCryptorCreate` 抓原始 32 字节密钥**（rmqg 方案，专门验证过 4.1.11）；
- **只需重签微信，不需要关 SIP**（实测 attach+读内存均正常）；
- 抓密钥时**微信必须运行且已登录**，且要用户持续滚动聊天触发数据库解密，否则抓到 0 个密钥。

## 阶段 B：分析（拿到聊天记录后）

详细框架见 `references/analysis-framework.md`。核心动作：

1. **完整阅读**：聊天量大（>5000 条）时，派 subagent 阅读并输出结构化报告，主会话同时读其余文件，最后合并。
2. **收集线下信息**：主动问用户补充"见面细节/资料卡/观察到的疑点"，线下信息与聊天记录**交叉验证**（这是本方法的灵魂）。
3. **按八维框架输出**：时间线主动度 / 沟通风格 / 身份职业经济 / 婚恋观孩子观 / 边界感 / 契合度 / 风险信号 / 总体判断。
4. **多目标时出横向对比表**：统一维度打分 + 一句定位 + 明确排序建议。
5. **给可执行动作**：验真问题清单、试探话术、底线进度表——落到"下一句话怎么说、下一个动作怎么做"。

## 交付物清单（视需求选做）

| 交付物 | 适用场景 |
|---|---|
| 单人深度画像（八维 + 风险/加分 + 下一步） | 分析某一个人 |
| 横向对比表 + 排序建议 | 对比 2~N 个候选人 |
| 验真问题清单（一句话版本） | 用户要"怎么试探/怎么问" |
| 约会推进话术脚本 | 用户要"怎么追/怎么推进" |
| dating 日记 / 小红书文风改写 | 用户要复盘发帖 |
| 按人打包的 txt+CSV+zip | 分对话框单独分析 |

## 快速参考

- 技术细节/踩坑全录 → `references/export-playbook.md`
- 分析框架/话术模板/日记文风 → `references/analysis-framework.md`
- 可复用脚本 → `scripts/`（setup.sh / capture.sh / diag_test.py）
